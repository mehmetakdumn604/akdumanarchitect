// Uygulama içerisinde bulunan temalar
/// Enumeration of available application themes.
/// 
/// This enum defines the different theme modes that can be applied
/// to the application's user interface.
enum AppThemes {
  /// Light theme mode with bright background and dark text
  light,
  
  /// Dark theme mode with dark background and light text
  dark
}
